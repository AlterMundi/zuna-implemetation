# Core dependencies (local machine)
numpy>=1.19.0
scipy>=1.5.0
mne>=1.6.0
python-osc>=1.7.0
mido>=1.3.0

# Testing
pytest>=7.0.0

# ZUNA model (GPU machine only)
# Uncomment on the machine that runs the model:
# zuna
